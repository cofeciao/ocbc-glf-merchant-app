export type IPercentageTextField = {
  register?: Function;
  errors?: any;
  name: string;
  adornment: string;
  dataRedux: string;
  description: string;
  label: string;
  helperText: string;
  required?: boolean;
};
